{
    "compilerOptions": {
        "target": "ES6",
        "module": "esnext",
        "baseUrl": "./src", // all paths are relative to the baseUrl
        "allowSyntheticDefaultImports": true,
    }
}